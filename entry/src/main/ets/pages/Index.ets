// 临时占位：阶段 4 会替换成组件画廊 + 实战页
@Entry
@Component
struct Index {
  build() {
    Column() {
      Text('unilist gallery').fontSize(18)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
